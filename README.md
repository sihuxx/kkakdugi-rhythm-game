# 꺅두기 하우스 🍀

꺅두기를 **애기부터 어른까지 키우는** 웹 게임. 집에서 밥 주고 씻기고 재우면 두기가 자라고,
현관으로 나가면 **리듬게임**과 **꺅두기런**으로 클로버를 벌어 가구와 새 모습을 사 옵니다.

브라우저만 있으면 돌아가고, 설치할 것도 서버도 없습니다.

```
index.html 을 브라우저로 더블클릭
```

<!-- 스크린샷을 넣으려면 docs/ 폴더에 이미지를 두고 아래 줄의 주석을 푸세요
![집 화면](docs/home.png)
-->

## 놀이 흐름

**집에서 돌보기 → 외출해서 벌기 → 사고 꾸미기 → 두기가 자람**

1. 집에서 `WASD`(또는 방향키)로 걸어다니다가 가구 앞에서 `E`
2. 밥 주기 · 물 주기 · 씻기기 · 놀아주기 · 재우기 · 청소하기 — 할 때마다 **경험치**가 쌓이고 스탯이 찹니다
3. 현관(`E`)이나 위쪽 **외출** 버튼으로 미니게임 → **클로버 + 경험치**
4. 미니게임을 다녀오면 배고프고 지저분해져요. 다시 돌봐주면 됩니다
5. 클로버로 **가구**를 사고(상점), **새 모습**을 뽑고(뽑기 기계), 옷장에서 갈아입혀요

### 성장 4단계

| 단계 | 필요 경험치 |
| --- | --- |
| 애기 두기 | 0 |
| 어린 두기 | 90 |
| 청소년 두기 | 280 |
| 어른 두기 | 700 |

자랄수록 화면 속 두기가 커지고, 꺅두기런에서도 그만큼 커집니다.

### 스탯 4가지

배부름 · 깨끗함 · 기분 · 기운. 평균이 **컨디션**이고, 컨디션이 높을수록 미니게임 보상이 늘어납니다
(보상 배율 = 0.65 + 0.45 × 컨디션, 가구 보너스는 여기에 더 곱해집니다).

### 가구

상점에서 클로버로 삽니다. 사면 집에 바로 놓이고 효과도 붙어요 — 러그(기분 회복 +2),
램프(잘 때 기운 +4), 책장(경험치 +8%), 티비(놀아줄 때 기분 +5), 냉장고(밥 배부름 +6),
액자·벽시계(미니게임 보상 +5%), 장식 깃발(기분 +3), 생일 케이크(경험치 +15%).

## 미니게임 ① 리듬게임

위에서 내려오는 네 줄 노트를 **A · S · K · L** 로 받아냅니다. 곡 4개 × 난이도 4단계.

| 조작 | 하는 일 |
| --- | --- |
| `A` `S` `K` `L` | 네 줄 노트 받기 (설정에서 바꿀 수 있어요) |
| 길게 누르기 | 쓰왜 — 롱노트를 끝까지 잡기 |
| `Esc` | 일시정지 (3·2·1 카운트다운 후 재개) |

네 곡 모두 이 페이지가 **직접 연주하는 창작 칩튠**이라 외부 음원이 없고, 채보와 반주가 같은
데이터에서 나와 싱크가 어긋나지 않습니다. 판정은 PERFECT / GREAT / GOOD / MISS.

## 미니게임 ② 꺅두기런

쿠키런 같은 2D 횡스크롤 러닝. 임시 맵 한 판(약 2,200m)이 들어 있습니다.

| 조작 | 하는 일 |
| --- | --- |
| `Space` / `↑` / `W` | 점프 (공중에서 한 번 더 누르면 2단) |
| `↓` / `S` | 슬라이드 — 덩굴 밑을 지나갑니다. 공중에서 누르면 쿵 내려찍기 |
| `E` | 스킬 (게이지가 다 차면) |
| `Esc` | 그만두고 집으로 |

젤리(네잎클로버)를 먹으면 점수와 스킬 게이지가 차고, 체력 클로버 3개가 다 떨어지면 끝.
깃발까지 달리면 보너스를 받습니다.

**모습마다 스킬이 다릅니다** — 돌진(무적·장애물 파괴) / 폭신 점프(3단) / 젤리 자석 /
둥실둥실(활공) / 느긋느긋(느리게·젤리 2배) / 꽉 버티기(보호막).

## 뽑기와 옷장

뽑기 기계에서 클로버로 새 **모습**을 뽑습니다(1회 110, 10연차 1000). 10연차에는 귀함 이상이
하나 확정이고, 겹치면 클로버로 돌려받아요. 뽑은 모습은 **옷장**에서 갈아입힙니다 — 두기는
한 마리, 모습만 바뀝니다. 도감을 모으면 클로버와 노트 스킨을 받습니다(5·9·13·17종).

## 폰에서

집에서는 가고 싶은 곳을 탭하면 두기가 걸어가서 바로 상호작용합니다.
리듬게임은 네 줄을 손가락으로 두드리고, 꺅두기런은 화면 위쪽=점프 / 아래쪽=슬라이드,
아래 버튼으로도 됩니다.

## 파일 구조

```
index.html          화면 구조 (집 + 모든 화면이 한 페이지)
css/style.css       전체 스타일
js/boot.js          캔버스·크기·선 굵기 같은 공용 도구
js/data.js          모습 17종, 곡 4개, 난이도, 표지 그림
js/grow.js          성장 단계 · 가구 · 돌봄 · 런 스킬 표
js/save.js          세이브(ggakdugi.v2) · 클로버 · 경험치 · 스탯
js/chart.js         채보 생성 — 멜로디에서 노트를 뽑아낸다
js/audio.js         Web Audio 반주·효과음
js/world.js         리듬게임 배경 테마
js/menus.js         옷장 · 상점 · 뽑기 · 설정 · 외출 모달
js/cut.js           뽑기 연출 (인트로 → 공개)
js/home.js          집 — 방, 가구 그리기, 돌봄
js/rhythm.js        리듬게임
js/run.js           꺅두기런 (DugiRun 모듈)
js/app.js           셸 — 화면 전환, 입력, 메인 루프
assets/*.png        꺅두기 그림
tools/build_single.py  파일 하나로 묶기
```

스크립트는 **읽히는 순서가 중요합니다** (boot → data → grow → save → chart → audio → world →
menus → cut → home → rhythm → run → app). `index.html` 아래쪽 순서를 바꾸지 마세요.

### 파일 하나로 묶기

```
python tools/build_single.py
```

`dist/ggakdugi-house.html` 한 장이 나옵니다. 이미지가 전부 안에 박혀 있어서 그 파일만 보내도
그대로 돌아가요.

### GitHub Pages로 올리기

1. 이 폴더를 통째로 저장소에 올립니다
2. 저장소 → **Settings → Pages**
3. Source를 **Deploy from a branch**, 브랜치 `main` / 폴더 `/ (root)`
4. 1~2분 뒤 `https://<아이디>.github.io/<저장소이름>/` 에서 열립니다

## 고치고 싶을 때

- **가구 추가** — `js/grow.js`의 `FURNITURE`에 한 줄 넣고, `js/home.js`의 `drawFurn()` 스위치에
  그리는 법을 추가하면 상점에도 자동으로 나옵니다
- **돌봄 종류 추가** — 같은 파일의 `CARE`에 넣고, 가구의 `act`를 그 이름으로
- **런 맵 고치기** — `js/run.js` 위쪽 `CHUNK`(덩어리 하나)와 `MAP`(이어 붙이는 순서).
  땅과 땅 사이를 비우면 구덩이가 됩니다. 한 번 점프로 높이 약 200 · 거리 330을 넘어요
- **곡 추가** — `js/data.js`의 `SONGS`에 `mkSong({...})` 한 덩어리. 멜로디만 적으면 반주와
  네 난이도 채보가 자동으로 만들어집니다
- **선 굵기** — 게임 안의 모든 선은 `js/boot.js`의 `LW()` 하나로 정해집니다

## 만든 것들

- **음악** — 네 곡 모두 이 페이지가 직접 연주하는 창작 칩튠. 외부 음원 없음
- **그림** — 꺅두기 ⓒ 박시후. 게임에 넣으면서 외곽선을 살려 배경만 지우고 크기를 맞췄습니다

## 라이선스

코드는 [MIT](LICENSE). **`assets/`의 꺅두기 그림은 제외** — 저작자에게 권리가 있습니다.
